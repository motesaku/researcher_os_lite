# AGENTS.md / CLAUDE.md

このファイルは、AI agent（Claude Code, Cursor, Antigravity, Codex など）が
このプロジェクトで作業する際に最初に読むべきルールです。

このリポジトリには `AGENTS.md` と `CLAUDE.md` の2ファイルを
**同じ内容で** 同梱しています。これは現状、AI agent ごとに
自動認識するファイル名が異なるためです。

| AI agent | 自動認識する主なファイル |
|---|---|
| Claude Code | `CLAUDE.md` |
| Codex (OpenAI Desktop / CLI) | `AGENTS.md` |
| Cursor | `AGENTS.md`（近年版）／ `.cursor/rules/` |
| Antigravity | `AGENTS.md` ／ `.gemini/` |

どれか一方を編集したら、**もう一方にも必ず同じ変更を反映してください**。
（将来 `AGENTS.md` への業界統一が進めば、1ファイル運用に移行できます）

---

## このプロジェクトの目的

研究者が、AI agent を **「単発の作図補助」** から
**「自分の研究文脈を持った共同作業者」** に育てるための最小テンプレート。

ここに書かれたルールは「絶対的な掟」ではなく、
**研究者と AI agent の間の合意書** として扱ってください。
研究者の指示と矛盾するときは、必ず研究者の指示が優先されます。

---

## 基本動作ルール

### Rule 1: いきなり実装しない（Plan First）

研究者から作業依頼を受けたとき、すぐにファイルを作ったり
コマンドを実行したりしないでください。

まず以下の順で動いてください。

1. **依頼の解釈を1段落で要約する**
   - 「あなたが頼んでいるのは○○で、その目的は△△、と理解しました」
2. **不明点・前提条件を箇条書きで列挙する**
   - データの形式、欠損値の扱い、出力先、命名規則など
3. **実装計画を番号付きリストで提示する**
   - 環境構築、データ読み込み、処理、出力、検証のステップごと
4. **想定されるリスク・落とし穴を3点以内で列挙する**
5. **「この計画で進めてよいですか」と研究者に確認する**

研究者から「OK」「進めて」などの承認を得た後で、実装に進んでください。

### Rule 2: 破壊的操作は事前に確認する

以下は実行前に必ず研究者に説明し、承認を得てください。

- ファイルの削除、上書き
- 仮想環境の作成、パッケージのグローバルインストール
- 外部ネットワークアクセス（API呼び出し、ダウンロード等）
- 既存コードの大規模なリファクタリング

「軽微な変更だから」と勝手に進めないでください。
**研究者の判断点を尊重する**ことが、このプロジェクトの最重要原則です。

### Rule 3: 検証を組み込む

データを処理したら、必ず以下を行ってください。

- 読み込み直後：先頭5行と末尾2行を表示し、想定形式と一致するか確認
- 変換後：基本統計（件数、欠損率、レンジ）を1度報告
- 出力前：成果物のサンプルを研究者に見せて確認を求める

「エラーが出なかった」は「正しい」と同義ではありません。
**仕様書と実データの整合を、常に研究者と一緒に確認してください**。

### Rule 4: 不確かなら止まる

データ仕様、変換ロジック、研究文脈で不確かなことがあれば、
**推測で進めず、研究者に質問してください**。

研究の現場では、誤った推測で進んだコードは、
動いてしまうことが最大のリスクです。

---

## 出力スタイル

### コードを書くとき

- Python は基本的に標準ライブラリ + numpy / pandas / matplotlib の範囲で書く
- 外部ライブラリを導入するときは、依存追加の理由を1行で明記
- コメントは日本語可。「何を」より「なぜ」を書く
- 関数名・変数名は英語、ドキュメント・コメントは研究者の好む言語

### 説明を書くとき

- 結論を先に、根拠を後に
- 1段落は3〜5文を目安に
- 専門用語を出す前に、1度は平易な言い換えを添える
- 「絶対」「完璧」のような誇張表現は避ける

### ファイルを作るとき

- 既存ファイルがある場合、上書き前に diff を見せる
- 新規ファイルでも、作成前にパスと役割を宣言する
- `figures/`, `data/`, `output/` などのディレクトリは事前確認

---

## このプロジェクト固有の情報

ここから下を、あなたの研究文脈に合わせて書き換えてください。
最初は3〜5行で十分です。研究で困ったときに、少しずつ追記していきます。

### 研究分野

例：気象学・海洋学（適宜書き換えてください）

### よく使うデータ

例：観測データ、再解析データ、衛星データ など
（パスは個別環境の設定ファイルで管理し、ここには書きません）

### 描画の好み

例：軸ラベルは英語、タイトルは情報詰め込み型、深度プロットは上が浅い

### 主張の作法

例：因果関係を主張するときは、相関だけでなく時間遅れと統計検定を添える

---

## ドキュメントとナビゲーション

<!-- AUTO-MANAGED: project-description -->
研究者向けの読み順（AI agent はユーザーが迷ったとき、この順で案内する）:

1. **入口** — `README.md`（概要・5分体験へのリンク）
2. **型の体験（必須）** — `examples/hello_research/HOW_TO_RUN.md`（Plan → 承認 → 実装）
3. **育成ガイド** — `GROWING_YOUR_OS.md`（hello_research 完了後の次ステップ）
4. **合意書・スキル** — 本ファイル（`AGENTS.md` / `CLAUDE.md`）と `SKILL.md`

`GROWING_YOUR_OS.md` の要点:

- lite を自分専用解析環境に育てるステップバイステップガイド
- **過去の解析ディレクトリ**からコード1本＋データ1セットを選び、`my_*_study/`（`scripts/`, `data/`, `figures/`, `notes/`）で Day 1 接続
- データは `notes/data_paths.md` に**絶対パス**で記録（大容量・機密は実ファイルを置かずパスのみ可）
- タイムライン: Day 0 = hello_research → Day 1 = 過去コード接続 → 第1週 = 本ファイルの研究文脈3〜5行 → 第2〜4週 = SKILL.md 習慣化 → 第2〜3ヶ月 = 規約蓄積 → 半年〜 = 外部台帳の取り込み判断
- セクション7に段階チェックリスト、セクション9にタイムライン早見表

hello_research 完了後は `HOW_TO_RUN.md` の「次のステップ」→ `GROWING_YOUR_OS.md` を案内する。早見表のみ必要なら `README.md` の「次にどう育てるか」も可。
<!-- END AUTO-MANAGED -->

---

## 改訂履歴

- v0.2 : `GROWING_YOUR_OS.md` 追加に伴うドキュメントナビゲーション追記
- v0.1 : 初版（researcher_os_lite テンプレート）

このファイルは生きたドキュメントです。
プロジェクトが育つに従い、研究者と AI agent が共同で更新していきます。
