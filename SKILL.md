# SKILL.md

このプロジェクトで AI agent が扱える「スキル」を定義します。

スキル定義は、AI agent に対して
**「何を頼んでよくて、何は頼まない方がよいか」を事前に共有する**
仕組みです。これにより、研究者は「これは頼めるかな」と毎回迷う必要がなくなります。

最初は **3つだけ** にしてあります。
研究で本当に必要になったときに、少しずつ追加していくのが推奨の運用です。

---

## Skill 1 : data_inspection（データ点検）

研究者が新しいデータファイル（CSV, txt, NetCDF, PDF仕様書 など）
を渡したとき、その構造と内容を点検する。

### できること

- ファイル形式の自動判別と先頭・末尾の表示
- カラム構成、データ型、欠損値の検出と報告
- 仕様書（README, PDF など）と実データの整合確認
- データ範囲、サンプル間隔、欠損率の基本統計の報告
- 不審な点（外れ値、改行コード、文字エンコード）の指摘

### できないこと（頼まない方がよいこと）

- データの真偽の判断（実観測との一致など）
- 専門領域の知識を要する物理的妥当性の判断
- 大規模ファイル（> 1GB）の全行スキャン

### 使用例

```
このCTDデータの中身を点検してください。
仕様書PDFも一緒に置いてあります。
```

→ AI agent は仕様書を読み、実データと突き合わせ、整合状況と
不審点を報告する。問題があれば研究者の判断を仰ぐ。

---

## Skill 2 : analysis_planning（解析計画）

研究者の漠然とした依頼から、実装前に詳細な計画を立てる。

### できること

- 依頼の解釈と要約
- データ処理、解析、可視化の手順を番号付きで設計
- 想定リスクと検証ポイントの列挙
- 必要な環境構築、依存ライブラリの提案
- 出力ファイルの命名と保存先の提案

### できないこと

- 研究上の判断（どの統計検定を使うべきか等）の最終決定
- 研究者の好みやスタイルを推測すること（AGENTS.md / CLAUDE.md に書いてある範囲で対応）

### 使用例

```
data/ にCTDデータがあります。
水温と塩分の時間-深度断面を描いてください。
まず計画を立ててから進めてください。
```

→ AI agent は計画を Markdown 番号付きリストで提示し、
研究者の承認を待つ。承認後に実装に進む。

---

## Skill 3 : reproducible_implementation（再現可能な実装）

承認された計画に従って、コードと出力を生成する。

### できること

- Python スクリプトの作成（標準ライブラリ + numpy/pandas/matplotlib 中心）
- requirements.txt の作成と必要パッケージの宣言
- 中間ファイルと最終出力ファイルの分離
- 各ステップでの検証コードの埋め込み
- エラー時の原因説明と修正提案

### できないこと

- 環境変数や認証情報の自動設定
- 研究者の許可なく外部APIを呼ぶこと
- ローカル環境固有のパス問題の完全自動解決

### 使用例

```
さっきの計画でOKです、進めてください。
```

→ AI agent は計画通りに実装を進める。途中で計画から外れる必要が
生じた場合は、1行で説明してから進める。

---

## スキルの追加について

新しいスキルを追加するときは、以下のテンプレートを使ってください。

```markdown
## Skill X : skill_name（日本語名）

何のためのスキルか、1〜2文で説明。

### できること
- 箇条書き

### できないこと
- 箇条書き

### 使用例

(自然言語の依頼例と、その挙動の説明)
```

### スキル追加の判断基準

- そのスキルを「3回以上頼んだ」経験があるか？（あれば追加する価値がある）
- できないことを書けるか？（書けないなら、まだスキル化は早い）
- 1人で完結するか？（複数のスキルにまたがるなら、分解する）

研究は試行錯誤の連続です。**3回繰り返したパターンが、スキルになる**
というのが、現場で持続する基準です。

---

## 改訂履歴

- v0.1 : 初版（data_inspection, analysis_planning, reproducible_implementation）
