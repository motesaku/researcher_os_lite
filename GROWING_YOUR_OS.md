# 研究者OSの育て方 — ステップバイステップガイド

このドキュメントは、`researcher_os_lite` を **自分専用の解析環境** に育てていくための道筋です。

最小サンプル [`examples/hello_research/HOW_TO_RUN.md`](examples/hello_research/HOW_TO_RUN.md) で「Plan → 承認 → 実装」の**型**を体験したあと、**過去の解析コードやデータ**を参照しながら、何をいつ足せばよいかを順番に示します。

完璧を目指さず、**困ったときに少し足す**。それが唯一持続する育て方です。

---

## 0. 全体像 — lite は何の「種」か

`researcher_os_lite` は、より大きな研究者OS設計（Dataset Registry、PastWorks、Provenance Logger など）の**根幹部分だけを抽出した出発点**です。

あなたの環境では、参照先のパスやディレクトリ名は人それぞれ異なります。このガイドでは、次のような**汎用的な呼び方**を使います。

| 呼び方 | 意味（例） |
|---|---|
| **過去の解析ディレクトリ** | 以前の研究で使った `scripts/`、`analysis/`、論文用フォルダなど |
| **データ置き場** | 観測CSV、再解析NetCDF、論文付録データなどが入っている場所 |
| **このリポジトリ（lite）** | 今 clone/fork した `researcher_os_lite` のルート |

### lite に最初からあるもの

| ファイル | 役割 |
|---|---|
| `AGENTS.md` / `CLAUDE.md` | AI agent への基本ルールと、**あなたの研究文脈**を書く場所 |
| `SKILL.md` | AI agent に「何を頼んでよいか」を定義するスキルシート |
| `examples/hello_research/` | 5分で型を体験するサンプル |

### 将来、必要になったら足せるもの（参考）

研究が進むと、次のような仕組みが欲しくなることがあります。**今すぐ全部入れる必要はありません。**

| 概念 | ざっくりした役割 | 足すタイミングの目安 |
|---|---|---|
| データ台帳 | データIDとパスを一元管理 | 同じデータを3回以上、パスを毎回説明している |
| 過去資産台帳 | 過去プロジェクトの脚本・図・原稿への索引 | 過去論文の解析を「また使いたい」が月に数回ある |
| 再現ログ | いつ・何を・どの入力で実行したかの記録 | 共同研究者や未来の自分が再現を求め始めた |
| 出力契約・論理監査 | 図表や主張の形式・強度を揃える | 論文執筆・査読対応が本格化した |

これらは、あなたや所属グループが別途育てている **過去の解析ディレクトリ** や、公開されている研究者OS実装から、**必要な部分だけ**取り込む想定です。参照先のパスは環境ごとに異なるため、この lite 単体の手順だけで完結できます。

---

## 1. 事前準備（約15分）

### Step 1-1 : リポジトリを手元に置く

```bash
git clone <あなたのリポジトリURL> researcher_os_lite
cd researcher_os_lite
```

自分専用に育てるなら、fork してから clone するか、clone 後に remote を自分のリポジトリに差し替えてください。

### Step 1-2 : hello_research で型を体験する

[`examples/hello_research/HOW_TO_RUN.md`](examples/hello_research/HOW_TO_RUN.md) の手順どおり、

1. AI agent をこのリポジトリで起動
2. `PROMPT.md` を貼り付け
3. **Plan をレビュー**してから承認
4. `figures/hello.png` が出ることを確認

ここまでが **「研究者OSを使う」最低条件** です。型が体験できていなければ、自分のデータに進まないでください。

### Step 1-3 : 取り込みたい過去資産を1つ選ぶ

次の条件を満たすものを **1セットだけ** 選びます。

- **解析コード 1本**（`.py`、`.R`、Jupyter notebook など。Python 推奨）
- **そのコードが参照しているデータ 1つ**（CSV、NetCDF、txt など）

「いちばんよく使う」「最近また触りたい」「共著者に説明しやすい」のいずれかで選べば十分です。全部を一度に移す必要はありません。

---

## 2. 最初の一歩（Day 1）— 過去コードを lite に接続する

### Step 2-1 : 自分用の作業ディレクトリを作る

lite のルートに、研究プロジェクト用のディレクトリを1つ作ります。名前は自由です。

```bash
# 例: プロジェクト名 my_first_study
mkdir -p my_first_study/{scripts,data,figures,notes}
```

| ディレクトリ | 用途 |
|---|---|
| `scripts/` | 解析スクリプト（過去コードのコピーまたはリンク） |
| `data/` | このプロジェクトで使うデータ（またはパスメモ） |
| `figures/` | 出力図 |
| `notes/` | AI とのやり取りメモ、データ仕様メモ |

**データが大きい・機密がある場合**は、`data/` に実ファイルを置かず、`notes/data_paths.md` に絶対パスだけ書いても構いません。

### Step 2-2 : 過去の解析コードを置く

**過去の解析ディレクトリ**から、選んだスクリプトを `my_first_study/scripts/` にコピーします。

```bash
cp /path/to/your/past_project/plot_something.py my_first_study/scripts/
```

元のディレクトリはそのまま残してください。lite は「索引と作業場」であり、過去資産の置き換えではありません。

### Step 2-3 : データの場所を AI agent が分かる形で残す

`my_first_study/notes/data_paths.md` を作成し、次を書きます。

```markdown
# データパス（このプロジェクト）

## 主データ
- ファイル: /absolute/path/to/your/data.csv
- 形式: CSV（列: time, value, ...）
- 出典: （観測名、論文、ダウンロード元など1行）

## 注意
- 欠損値: （わかれば）
- 機密: 外部に出さない / 共著者のみ など
```

パスは **必ず絶対パス** にしてください。AI agent は相対パスを誤解しやすいです。

### Step 2-4 : AI agent に「点検 → 計画」を依頼する

lite のルート（または `my_first_study/`）で AI agent を起動し、以下をコピペして送ります。

```
my_first_study/ に、過去の解析 scripts/plot_something.py と
notes/data_paths.md に書いたデータがあります。

まず AGENTS.md（または CLAUDE.md）と SKILL.md の Rule 1 に従い、
いきなり実装せず計画を出してください。

やってほしいこと:
1. scripts/plot_something.py の内容を読み、何をしているか要約
2. data_paths.md のデータを点検（先頭・末尾、列構成、欠損）
3. このスクリプトを lite の作法（Plan First、検証、figures/ 出力）に
   合わせて動かすための手順を番号付きで提案
4. 不明点があれば質問

破壊的操作（元ファイルの上書き、大量インストール）は事前に確認してください。
```

**ここで止まる。** Plan が返ってきたら、hello_research と同様に3分レビューします。

- 依頼の解釈は正しいか
- データパスを読んでいるか
- リスク（上書き、機密、ライブラリ追加）に触れているか

問題があれば自然言語で修正を依頼し、納得したら「OK、進めてください」と伝えます。

### Step 2-5 : 最初の成果物を確認する

成功の目安:

- `figures/` に期待どおり（または近い）出力がある
- AI agent が処理の件数・欠損率などを報告している
- 元の過去ディレクトリは変更されていない

うまくいかなくても失敗ではありません。**詰まった点を `notes/` に1行メモ** しておくと、次の Step で `AGENTS.md` に昇格できます。

---

## 3. 第1週 — 研究文脈を3〜5行書く

hello_research と Day 1 が終わったら、`AGENTS.md`（`CLAUDE.md` も同内容）の **「このプロジェクト固有の情報」** を書き換えます。

### 書く内容（テンプレート）

```markdown
### 研究分野
（例: 大気海洋学 / 地域気候 / 資料同化 — 1行）

### よく使うデータ
（例: ERA5再解析、自船CTD、地上観測 — 固有名詞だけ。パスは notes/ や SKILL に）

### 描画の好み
（例: 軸ラベル英語、深度は上が浅い、カラーバーは jet 使わない）

### 主張の作法
（例: 因果は相関だけで書かない、統計検定名を図キャプションに入れる）
```

**3〜5行で十分** です。空欄のままでも動きますが、1週間後には必ず1項目でも埋めてください。

### AI agent への依頼例（任意）

```
AGENTS.md の「このプロジェクト固有の情報」を、
my_first_study/notes/ にあるメモを参照しながら
3〜5行で埋めてください。変更前に diff を見せて、承認を待ってください。
```

---

## 4. 第2〜4週 — SKILL.md とディレクトリ習慣

### 4-1. 同じ依頼を3回したら SKILL に昇格

`SKILL.md` には最初から3スキル（data_inspection, analysis_planning, reproducible_implementation）があります。

**同じパターンの依頼を3回以上したら**、新スキルを1つ追加します。例:

- 「論文用 figure を 300 dpi PNG + PDF 両方で出す」
- 「イベント検出の前に必ず QC サマリを出す」
- 「NetCDF の座標名を CF 規約で確認する」

追加するときは `SKILL.md` 末尾のテンプレート（できること / できないこと / 使用例）を使います。

### 4-2. プロジェクト README を1枚足す

`my_first_study/README.md` に、次だけ書けば十分です。

```markdown
# my_first_study

## 目的
（1文）

## データ
notes/data_paths.md を参照

## 再現手順
1. （環境: python 3.x, 必要なら venv）
2. `python scripts/plot_something.py`（または AI が整えた入口）
3. 出力: figures/

## 最終更新
YYYY-MM-DD — （何をしたか1行）
```

未来の自分と AI agent の両方の索引になります。

### 4-3. 第2プロジェクトを増やすタイミング

次の論文・次の観測キャンペーンが始まったら、`my_second_study/` を同じ型で追加します。

lite ルートの `AGENTS.md` / `SKILL.md` は **全プロジェクト共通**、各 `my_*_study/` は **プロジェクト固有** — この二層にすると散らかりにくいです。

---

## 5. 第2〜3ヶ月 — 規約と再現性を少しずつ

無理に全部やらず、**困ったときに1つ** 足してください。

| 困ったこと | lite で足す場所 | 例 |
|---|---|---|
| 毎回パスを説明している | `notes/data_paths.md` または将来のデータ台帳 | dataset_id を自分で命名 |
| 図の体裁がバラバラ | `AGENTS.md` の描画の好み | フォントサイズ、dpi、ファイル名規則 |
| 主張の強さが曖昧 | `AGENTS.md` の主張の作法 | 「示唆」vs「確認」の使い分け |
| 同じ処理を何度も頼む | `SKILL.md` | 上記「3回ルール」 |
| 数ヶ月後に再現できない | 各プロジェクトの `README.md` | 実行コマンドと出力一覧 |

### 依頼テンプレート: 再現性の整理

```
my_first_study/ の scripts/ と figures/ を見て、
再現手順を README.md に追記する計画を立ててください。
使ったライブラリ、データパス、出力ファイル名を含めてください。
実装前に計画を見せてください。
```

---

## 6. 半年以降 — 過去の解析ディレクトリから取り込む判断

より大きな研究者OS（データ台帳、過去資産台帳、再現ログなど）は、**あなたの過去の解析ディレクトリ** やチームで共有されている実装から、必要な部分だけ取り込みます。参照先の URL やパスは環境ごとに異なるため、ここでは **判断基準** だけ示します。

| 取り込み候補 | 取り込む目安 | lite との関係 |
|---|---|---|
| データ台帳（YAML + ID） | データセットが10個を超え、パス説明に疲れた | `notes/data_paths.md` の発展形 |
| 過去資産台帳 | 過去論文の脚本・図を繰り返し参照する | 過去の解析ディレクトリへの索引 |
| 再現ログ | 共同研究で「いつ誰が何を実行したか」が問題になった | `README.md` + git 履歴の発展形 |
| 論理監査・主張台帳 | 論文投稿・査読が本格化 | `AGENTS.md` 主張の作法の発展形 |

**取り込まない判断も正解** です。lite のテキストファイル運用で足りているなら、そのまま続けてください。

---

## 7. チェックリスト — 今どの段階にいるか

### 入口（必須）

- [ ] hello_research を完了した（Plan をレビューしてから承認した）
- [ ] 自分用 `my_*_study/` ディレクトリを作った
- [ ] 過去コード1本 + データ1つで Day 1 の点検→計画→実行を1回した

### 第1週

- [ ] `AGENTS.md` の「プロジェクト固有の情報」を3行以上書いた
- [ ] `notes/data_paths.md` に絶対パスを書いた

### 第1ヶ月

- [ ] プロジェクト `README.md` がある
- [ ] `SKILL.md` に自分用の追加が1つある（または追加不要と判断した）

### 第3ヶ月

- [ ] 2つ目のプロジェクト、または同プロジェクトの第2解析が同じ型で回せた
- [ ] 「Plan を飛ばされた」ときに Rule 1 を参照して止められた

### 半年〜

- [ ] データ台帳・過去資産台帳など、外部仕組みを **必要なら** 検討した
- [ ] 取り込まないと決めたものも、理由を notes に1行残した

---

## 8. よくある失敗と対処

| 症状 | よくある原因 | 対処 |
|---|---|---|
| 毎回研究分野から説明している | `AGENTS.md` が空のまま | 第1週の3〜5行を書く |
| データが見つからないと言われる | 相対パス・曖昧な説明 | `notes/data_paths.md` に絶対パス |
| いきなりコードが書かれる | Rule 1 が読まれていない | 「まず計画を。AGENTS.md Rule 1」 |
| 過去ディレクトリが上書きされた | 破壊的操作の確認不足 | コピー先は `my_*_study/` のみに限定 |
| SKILL が100行になった | 早すぎるスキル追加 | 「3回ルール」に戻す |
| 環境が再現できない | requirements がない | プロジェクト README に pip 一覧を追記 |

---

## 9. タイムライン早見表

| 時期 | やること | 触るファイル |
|---|---|---|
| **Day 0** | hello_research | （サンプルのみ） |
| **Day 1** | 過去コード1本を接続 | `my_*_study/`, `notes/data_paths.md` |
| **第1週** | 研究文脈3〜5行 | `AGENTS.md` / `CLAUDE.md` |
| **第2〜4週** | 繰り返しパターンをスキル化 | `SKILL.md`, プロジェクト `README.md` |
| **第2〜3ヶ月** | 描画・主張・命名の規約 | `AGENTS.md`, 各 `notes/` |
| **半年〜** | 必要なら台帳・ログを外部から取り込み | 各自の過去解析ディレクトリ等 |

---

## 関連ドキュメント

- [README.md](README.md) — プロジェクト概要
- [examples/hello_research/HOW_TO_RUN.md](examples/hello_research/HOW_TO_RUN.md) — 5分サンプル
- [SKILL.md](SKILL.md) — スキル定義と追加テンプレート
- [AGENTS.md](AGENTS.md) / [CLAUDE.md](CLAUDE.md) — AI agent との合意書

---

## 改訂履歴

- v0.1 : 初版（lite 単体完結の育成ガイド）
